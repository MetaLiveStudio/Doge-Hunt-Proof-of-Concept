import { Entity, IEngine, PBAudioSource } from "@dcl/sdk/ecs";
export type Sounds = ReturnType<typeof createSounds>;
declare function createSounds(targetEngine: IEngine): {
    playSoundSegment: (entity: Entity, value: PBAudioSource, start: number, end: number) => void;
};
export declare const sounds: {
    playSoundSegment: (entity: Entity, value: PBAudioSource, start: number, end: number) => void;
};
export {};
